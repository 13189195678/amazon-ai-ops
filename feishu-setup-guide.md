# 烧烤炉 Amazon 运营看板 - 飞书搭建指南（修正版）

## 一、准备工作

### 1.1 创建飞书多维表格

1. 打开飞书 → 点击左侧"多维表格"
2. 点击"新建多维表格"
3. 命名为："烧烤炉 Amazon 运营数据中心"

### 1.2 创建 4 个数据表

在刚才创建的文件内，看底部标签栏，点击右侧的 **"+" 号**，选择"新建空白数据表"，重复操作，共创建 4 个数据表：

- 数据表 1：SKU 主数据
- 数据表 2：销售数据（每日）
- 数据表 3：库存数据
- 数据表 4：ASN 到货通知

> **这 4 个数据表都在同一个多维表格文件内**，底部点击标签可以切换

---

## 二、导入 CSV + 配置字段

### 2.1 导入 CSV 到对应数据表

切换到对应的数据表，点击右上角"..."→"导入"→"导入 Excel/CSV"，上传对应的 CSV 文件：

| 数据表 | 导入的 CSV 文件 |
|--------|---------------|
| SKU 主数据 | sku-master.csv |
| 销售数据（每日） | sales-daily.csv |
| 库存数据 | inventory.csv |
| ASN 到货通知 | asn.csv |

### 2.2 导入后：调整字段类型

导入后，检查每个字段的类型是否正确，如不正确则点击列头 → "修改字段类型"：

| 字段 | 正确类型 |
|------|---------|
| SKU编码 | 文本 |
| SPU | 文本 |
| 产品名称 | 文本 |
| 颜色 | 文本 |
| 箱型 | 文本 |
| 标价 | 货币（$）|
| 成交均价 | 货币（$）|
| 星级 | 数字 |
| Review数 | 数字 |
| 状态 | 文本 |
| 日期 | 日期 |
| 平台 | 文本 |
| 订单数 | 数字 |
| 销售件数 | 数字 |
| 销售额 | 货币（$）|
| 广告花费 | 货币（$）|
| 仓库 | 文本 |
| 可售数量 | 数字 |
| 待入库数量 | 数字 |
| 在途数量 | 数字 |
| 覆盖天数 | 数字 |
| 更新日期 | 日期 |
| ASN单号 | 文本 |
| 目标仓库 | 文本 |
| 总件数 | 数字 |
| ETA日期 | 日期 |
| 实际到货日 | 日期 |

---

## 三、设置关联字段和公式字段（关键步骤）

### 3.1 销售数据表：设置关联字段

1. 切换到 **"销售数据（每日）"** 数据表
2. 点击 SKU 列的列头 → "修改字段类型"
3. 选择 **"关联"**
4. 选择要关联的表："SKU 主数据"
5. 选择关联列："SKU编码"
6. 点击"确定"
7. 此时 SKU 列的值会自动匹配显示为 SKU 编码

#### 添加"查找引用"字段

关联设置完成后，添加以下"查找引用"字段：

1. 点击右侧"+"添加新字段
2. 字段类型选择 **"查找引用"**
3. 配置：

| 字段名 | 关联表 | 显示字段 |
|--------|--------|---------|
| SKU编码_引用 | SKU 主数据 | SKU编码 |
| SPU_引用 | SKU 主数据 | SPU |
| 产品名称_引用 | SKU 主数据 | 产品名称 |
| 颜色_引用 | SKU 主数据 | 颜色 |

#### 添加"公式"字段（ACOS）

1. 点击右侧"+"添加新字段
2. 字段类型选择 **"公式"**
3. 字段名：ACOS
4. 公式输入：
   ```
   广告花费 / 销售额
   ```
5. 数字格式：百分比

### 3.2 库存数据表：设置关联字段

1. 切换到 **"库存数据"** 数据表
2. 点击 SKU 列的列头 → "修改字段类型" → 选择 **"关联"**
3. 关联到："SKU 主数据" → "SKU编码"

#### 添加"查找引用"字段

| 字段名 | 关联表 | 显示字段 |
|--------|--------|---------|
| SKU编码_引用 | SKU 主数据 | SKU编码 |
| SPU_引用 | SKU 主数据 | SPU |
| 产品名称_引用 | SKU 主数据 | 产品名称 |
| 颜色_引用 | SKU 主数据 | 颜色 |

### 3.3 ASN 到货通知表：设置关联字段

1. 切换到 **"ASN 到货通知"** 数据表
2. 点击 SKU 列的列头 → "修改字段类型" → 选择 **"关联"**
3. 关联到："SKU 主数据" → "SKU编码"

#### 添加"查找引用"字段

| 字段名 | 关联表 | 显示字段 |
|--------|--------|---------|
| SKU编码_引用 | SKU 主数据 | SKU编码 |

#### 添加"公式"字段（ETA日期区间）

1. 点击右侧"+"添加新字段
2. 字段类型选择 **"公式"**
3. 字段名：ETA日期区间
4. 公式输入：
   ```
   IF(DAYS(ETA日期, TODAY()) < 0, "已到期/待处理", IF(DAYS(ETA日期, TODAY()) <= 7, "未来7天", IF(DAYS(ETA日期, TODAY()) <= 14, "8-14天", "15天后")))
   ```

---

## 四、配置仪表盘

### 4.1 创建仪表盘

1. 在多维表格右上角点击"+ 新建仪表盘"
2. 命名为："烧烤炉运营看板"

### 4.2 添加仪表盘组件

#### 组件 1：KPI 指标卡（6个）

添加"统计数字"组件：

| 指标名称 | 数据源 | 统计方式 | 字段 |
|---------|--------|---------|------|
| 本月销售额 | 销售数据（每日）表 | 汇总 | 销售额 |
| 本月订单数 | 销售数据（每日）表 | 汇总 | 订单数 |
| 平均 ACOS | 销售数据（每日）表 | 平均值 | ACOS |
| 可售库存总量 | 库存数据表 | 汇总 | 可售数量 |
| 在途库存总量 | 库存数据表 | 汇总 | 在途数量 |
| 有销 SKU 数 | SKU 主数据表 | 计数 | SKU编码 |

> 有销 SKU 数的筛选条件：在筛选器中添加"状态 = 在售"

#### 组件 2：销售趋势图

- 组件类型：**折线图**
- 数据源：销售数据（每日）表
- X 轴：日期
- Y 轴：销售额（汇总）
- 筛选：本月数据（日期 ≥ 本月1日）
- 图表样式：平滑折线

#### 组件 3：品类贡献饼图

- 组件类型：**饼图**
- 数据源：销售数据（每日）表
- 分类：SPU_引用（查找引用字段）
- 数值：销售额（汇总）

#### 组件 4：SKU 排行表

- 组件类型：**表格**
- 数据源：销售数据（每日）表
- 显示字段：
  - SKU编码_引用
  - 产品名称_引用
  - 颜色_引用
  - 销售额（汇总）
  - 订单数（汇总）
- 排序：销售额（降序）

#### 组件 5：库存分布图

- 组件类型：**条形图**
- 数据源：库存数据表
- X 轴：仓库
- Y 轴：可售数量（汇总）

#### 组件 6：ASN 到货时间轴

- 组件类型：**条形图**
- 数据源：ASN 到货通知表
- X 轴：ETA日期区间（公式字段）
- Y 轴：总件数（汇总）
- 排序：按 ETA日期区间升序（已到期→未来7天→8-14天→15天后）

---

## 五、各数据表最终字段清单

### 表 1：SKU 主数据（10个字段）

| 字段名 | 字段类型 |
|--------|---------|
| SKU编码 | 文本 |
| SPU | 文本 |
| 产品名称 | 文本 |
| 颜色 | 文本 |
| 箱型 | 文本 |
| 标价 | 货币 |
| 成交均价 | 货币 |
| 星级 | 数字 |
| Review数 | 数字 |
| 状态 | 文本 |

### 表 2：销售数据（每日）（13个字段）

| 字段名 | 字段类型 | 说明 |
|--------|---------|------|
| 日期 | 日期 | 导入 |
| SKU | 关联 | 导入后手动改成关联字段 |
| SKU编码_引用 | 查找引用 | 手动添加 |
| SPU_引用 | 查找引用 | 手动添加 |
| 产品名称_引用 | 查找引用 | 手动添加 |
| 颜色_引用 | 查找引用 | 手动添加 |
| 平台 | 文本 | 导入 |
| 订单数 | 数字 | 导入 |
| 销售件数 | 数字 | 导入 |
| 销售额 | 货币 | 导入 |
| 广告花费 | 货币 | 导入 |
| ACOS | 公式 | 手动添加：广告花费/销售额 |

### 表 3：库存数据（12个字段）

| 字段名 | 字段类型 | 说明 |
|--------|---------|------|
| SKU | 关联 | 导入后手动改成关联字段 |
| SKU编码_引用 | 查找引用 | 手动添加 |
| SPU_引用 | 查找引用 | 手动添加 |
| 产品名称_引用 | 查找引用 | 手动添加 |
| 颜色_引用 | 查找引用 | 手动添加 |
| 仓库 | 文本 | 导入 |
| 可售数量 | 数字 | 导入 |
| 待入库数量 | 数字 | 导入 |
| 在途数量 | 数字 | 导入 |
| 覆盖天数 | 数字 | 导入 |
| 更新日期 | 日期 | 导入 |

### 表 4：ASN 到货通知（10个字段）

| 字段名 | 字段类型 | 说明 |
|--------|---------|------|
| ASN单号 | 文本 | 导入 |
| SKU | 关联 | 导入后手动改成关联字段 |
| SKU编码_引用 | 查找引用 | 手动添加 |
| 目标仓库 | 文本 | 导入 |
| 总件数 | 数字 | 导入 |
| ETA日期 | 日期 | 导入 |
| ETA日期区间 | 公式 | 手动添加：区间计算 |
| 状态 | 文本 | 导入 |
| 实际到货日 | 日期 | 导入 |

---

## 六、检查清单

完成搭建后，逐项检查：

- [ ] 只有一个多维表格文件，叫"烧烤炉 Amazon 运营数据中心"
- [ ] 底部有 4 个数据表标签，可以切换
- [ ] "SKU 主数据"表有 10 条记录，包含"状态"字段
- [ ] "销售数据"表有 30 条记录，SKU 列已设为关联字段
- [ ] "销售数据"表有查找引用字段（SKU编码_引用、SPU_引用、产品名称_引用、颜色_引用）
- [ ] "销售数据"表有 ACOS 公式字段
- [ ] "库存数据"表 SKU 列已设为关联字段，有查找引用字段
- [ ] "ASN 到货通知"表 SKU 列已设为关联字段
- [ ] "ASN 到货通知"表有 ETA日期区间 公式字段
- [ ] 关联字段都正常显示数据（没有空值）
- [ ] 仪表盘有 6 个组件：KPI指标卡、折线图、饼图、表格、条形图×2
- [ ] 所有仪表盘组件正常显示数据

---

## 七、常见问题

### Q1：关联字段设置后没有匹配到数据？
A：确保关联的两个表都有相同的 SKU 编码值，且字段类型一致（都是文本）。

### Q2：查找引用字段显示空白？
A：先确认关联字段已正确匹配数据，查找引用依赖于关联字段。

### Q3：ACOS 公式报错？
A：检查"广告花费"和"销售额"字段是否为货币/数字类型，不能是文本。

### Q4：ETA日期区间公式报错？
A：检查"ETA日期"字段是否为日期类型，TODAY() 是系统内置函数不需要配置。

### Q5：仪表盘组件数据为空？
A：检查数据表是否有数据，筛选条件是否正确，字段类型是否匹配组件要求。

---

**祝您搭建顺利，大麦！**